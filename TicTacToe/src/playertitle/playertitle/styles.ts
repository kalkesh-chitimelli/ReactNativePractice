import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  humanContainerView: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  botContainerView: {flex: 1, alignItems: 'center', justifyContent: 'center'},
});
